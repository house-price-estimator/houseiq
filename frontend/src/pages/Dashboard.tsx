import { Box, Heading, Text } from "@chakra-ui/react";

export default function Dashboard() {
  return (
    <Box p={8}>
      <Heading mb={4}>Dashboard</Heading>
      <Text>Dashboard stats and overview will go here.</Text>
    </Box>
  );
}
