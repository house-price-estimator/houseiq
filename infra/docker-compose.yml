version: "3.9"
services:
  mongo:
    image: mongo:7
    ports: ["27017:27017"]

  ml:
    build: ../houseiq-ml
    ports: ["8000:8000"]

  backend:
    build: ../houseiq-backend
    ports: ["8080:8080"]
    depends_on: [ml, mongo]
